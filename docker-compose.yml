services:
  school-helper:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/school.db
      LOG_DIR: /app/data/logs
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:8080:8080"
